#ifndef CONFIGURE_HPP
#define CONFIGURE_HPP

// C/C++
#include <string>

// Test if this is windows system (WINDOWS or NOT_WINDOWS)
#define @WINDOWS_SYSTEM@

namespace Global {

std::string search_paths = "@MYPATH@";

};

#endif  // CONFIGURE_HPP
